Added mongo & catalogue
no change in mongo

changes in catalogue (url in server.js to mongo-service:27017)
rebuid image & push to hub
manifest written with new image

web -- added catalogue (url)
re build image
kubectl delete -f manifest.yaml
kubectl apply -f manifest.yaml

if load balancer taking long time
kubectl get svc // copy port
open port from inbound 
# external ip : port number

output :: we are able to see Categories on webpage